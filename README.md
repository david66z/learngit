Git is a distributed version control system.
~~this is nano~~ This is VScode
This is still VScode
Git is free software distributed under the GPL.
#####
~~~
import pytorch
import numpy as np
print("Hello, github,find your motiviation!.")
~~~

git track changes of files.

/ chapter 7.2
Creating a new branch is quick and simple. # 11.19
Git staging area(idex) is global, rather than per-branch. Only commits is per-branch.

~~~
init、add、commit、status、reset、push、pull、clone、reflog、log、remote、diff、checkout、rm、branch、merge、switch、stash、rebase、tag、show
~~~